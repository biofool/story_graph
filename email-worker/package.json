{
  "name": "story-graph-email",
  "version": "1.0.0",
  "description": "Cloudflare Email Worker that receives emails at story@magicsolutions.biz, extracts URLs, and stores them in KV for batch processing by the story_graph pipeline.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "wrangler": "^3.100.0"
  }
}
